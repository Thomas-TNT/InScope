<Window x:Class="InScope.QuestionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Question"
        Height="380" Width="440"
        MinHeight="320" MinWidth="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Padding="0,0,4,0">
            <StackPanel>
                <TextBlock Text="Question ID:" Margin="0,0,0,4"/>
                <TextBox Margin="0,0,0,12"
                         Text="{Binding QuestionId, UpdateSourceTrigger=PropertyChanged}"
                         MaxLength="64" ToolTip="Alphanumeric, underscores, hyphens (e.g. UsesPump)"/>

                <TextBlock Text="Question text:" Margin="0,0,0,4"/>
                <TextBox Margin="0,0,0,8"
                         Text="{Binding QuestionText, UpdateSourceTrigger=PropertyChanged}"
                         TextWrapping="Wrap" AcceptsReturn="True" Height="70" VerticalScrollBarVisibility="Auto"
                         ToolTip="The question shown to the user"/>

                <TextBlock Text="Sections (procedure types):" Margin="0,0,0,4"/>
                <ItemsControl ItemsSource="{Binding Sections}" Margin="0,0,0,8">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected}" Margin="0,4,0,0"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="Leave none selected for all sections."
                           FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,0"/>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="1" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Margin="0,16,0,0" Padding="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Save" Width="80" Height="28" Margin="0,0,8,0" IsDefault="True" Command="{Binding OkCommand}"/>
                <Button Content="Cancel" Width="80" Height="28" IsCancel="True" Command="{Binding CancelCommand}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
