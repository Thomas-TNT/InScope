<Window x:Class="InScope.BlockEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
        Title="Block Library Editor"
        Height="550" Width="800"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <CollectionViewSource x:Key="BlockListView" Source="{Binding Blocks}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Section"/>
            </CollectionViewSource.GroupDescriptions>
            <CollectionViewSource.SortDescriptions>
                <scm:SortDescription PropertyName="Section" Direction="Ascending"/>
                <scm:SortDescription PropertyName="BlockId" Direction="Ascending"/>
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" MinWidth="150"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*" MinWidth="200"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="8,8,8,4">
                        <TextBlock Text="Blocks" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <Button Content="Add" Margin="8,0,0,0" Padding="8,2" Command="{Binding AddBlockCommand}" IsEnabled="{Binding CanAddBlock}"/>
                        <Button Content="Delete" Margin="4,0,0,0" Padding="8,2" Command="{Binding DeleteBlockCommand}"/>
                    </StackPanel>
                    <ListBox x:Name="BlockList"
                             Grid.Row="2"
                             ItemsSource="{Binding Source={StaticResource BlockListView}}"
                             SelectedItem="{Binding SelectedBlock}"
                             DisplayMemberPath="BlockId"
                             Margin="4,0,4,4">
                        <ListBox.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}" Margin="0,8,0,4"/>
                                    </DataTemplate>
                                </GroupStyle.HeaderTemplate>
                            </GroupStyle>
                        </ListBox.GroupStyle>
                    </ListBox>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="{StaticResource SplitterBrush}" ShowsPreview="True"/>

            <Border Grid.Column="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="{Binding BlockFileNameText}" Margin="8,8,8,4" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <RichTextBox x:Name="DocumentEditor"
                                 Grid.Row="1"
                                 IsReadOnly="False"
                                 Padding="10"
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Auto"
                                 AcceptsReturn="True"
                                 Margin="4,0,4,4"/>
                </Grid>
            </Border>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="8,4" HorizontalAlignment="Right">
            <Button Content="Save" Width="80" Height="28" Margin="0,0,8,0" Command="{Binding SaveCommand}"/>
            <Button Content="Revert" Width="80" Height="28" Margin="0,0,8,0" Command="{Binding RevertCommand}"/>
            <Button Content="Close" Width="80" Height="28" IsCancel="True" Command="{Binding CloseRequestedCommand}"/>
        </StackPanel>

        <Border Grid.Row="2" Style="{StaticResource StatusBarAreaBorderStyle}">
            <TextBlock Text="{Binding StatusText}" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
        </Border>
    </Grid>
</Window>
